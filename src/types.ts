export type Product = {
  categoryID: number;
  discontinued: string;
  productID: number;
  productName: string;
  quantityPerUnit: string;
  reorderLevel: number;
  supplierID: number;
  unitPrice: number;
  unitsInStock: number;
  unitsOnOrder: string;
};
