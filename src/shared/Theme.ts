const Theme = {
  backgroundColor: "#FFF",
  fontFamily: "'Manrope', sans-serif",
};

type ThemeType = {
  backgroundColor: string;
  fontFamily: string;
};

export type { ThemeType };

export default Theme;
